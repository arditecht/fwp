/* $Id$ */

function DataInterface(){
	/*
	 * intercept all server calls, do manipulations, check connection, give offline readability
	 * if not for the entire lifetime of the client usage, then for the same render cycle
	 */
	
}